# AI Legal Contract Reviewer

**Segment classification for downstream clause explanation — a three-way comparison of GPT, RAG and Legal-BERT on English-law contracts.**

An LLM-powered contract reviewer built with a two-phase design methodology:

1. **Phase 1 — End-to-end ML pipeline.** Three architecturally distinct classifiers (zero-shot GPT 5.4 mini, GPT 5.4 mini + RAG, and a fine-tuned Legal-BERT) were trained/configured and evaluated on a custom, lawyer-annotated dataset of **3,548 contract segments** to find the best model for *contract segment classification* — building structural awareness of a contract (what is a clause, a heading, a definition, etc.).
2. **Phase 2 — Full-stack implementation.** The winning classifier is integrated into a deployed web application that extracts clauses from any uploaded contract PDF, then uses **GPT 5.4 with retrieved context (RAG)** to produce plain-English explanations and a client-facing risk analysis for each clause.

The project demonstrates that accurate segment classification unlocks downstream tasks — clause extraction, explanation and risk analysis — that could meaningfully reduce billable hours for legal practitioners.

Demo available at [Contract Reviewer](https://contract-reviewer-xyae8.ondigitalocean.app/)

**Send a message to my linkedin to get access!**

---

## Key Features

- **Secure login** — Supabase Auth (JWT sessions) gates access so only authorised users can trigger the document-analysis APIs.
- **PDF upload & in-browser viewing** — contracts are stored in DigitalOcean Spaces and rendered in the browser with `react-pdf` via presigned URLs.
- **Automatic segment classification** — every segment on the page is labelled into one of seven structural classes (clause, section heading, sub-heading, definition, preamble & parties, recitals, other) by the Phase 1 winning classifier, with a structured output parser enforcing valid labels.
- **Clause highlighting** — identified clauses are highlighted directly on the PDF, with a colour legend and arrows (`react-xarrows`) connecting each clause to its explanation.
- **Plain-English explanations** — each clause is explained at a level a complete layperson can understand, generated by GPT 5.4 grounded in context retrieved from the rest of the document.
- **Client risk analysis** — every explanation ends with a dedicated "Client Risk" assessment, reviewed for accuracy by a qualified UK lawyer and a law student.
- **Deployed proof of concept** — live on DigitalOcean App Platform for legal practitioner review.

---

## Table of Contents
 
- [Phase 1 — Finding the Best Classifier (End-to-End ML Pipeline)](#phase-1--finding-the-best-classifier-end-to-end-ml-pipeline)
  - [Dataset Construction & Annotation](#dataset-construction--annotation)
  - [Dataset Visualisation](#dataset-visualisation)
  - [Model Configurations — Three Experimental Conditions](#model-configurations--three-experimental-conditions)
  - [Fine-Tuning Legal-BERT](#fine-tuning-legal-bert)
  - [Results & The Winning Model](#results--the-winning-model)
- [Phase 2 — Production Implementation](#phase-2--production-implementation)
  - [Architecture](#architecture)
  - [Tech Stack](#tech-stack)
  - [How RAG Is Used](#how-rag-is-used)
  - [Deployment](#deployment)
- [Project Structure](#project-structure)
- [Evaluation Highlights](#evaluation-highlights)
- [Acknowledgements](#acknowledgements)

---

## Phase 1 — Finding the Best Classifier (End-to-End ML Pipeline)

The full research lifecycle: dataset construction → annotation → cleaning → visualisation → three controlled experiments → evaluation and model selection.

### Dataset Construction & Annotation

- **11 publicly available English-law contracts** (council tenancy agreements, Section 106 agreements, overage agreements, easements, right-to-manage documents) were sourced for non-commercial use.
- Each contract was scanned with **Azure Document Intelligence**, extracting segments with positional metadata into JSON.
- A **qualified UK lawyer and a second-year law student** annotated every segment in **Label Studio**, self-hosted on a DigitalOcean Droplet, against an agreed seven-class taxonomy grounded in prior literature.
- Annotations were cleaned and normalised with **pandas** (`experiments/clean_data.ipynb`): label simplification, column pruning and per-contract CSV export.
- The data was **split by document** — not by row — to prevent cross-contamination between sets:

| Split | Contracts | Segments |
|---|---|---|
| Training | 6 | 2,354 |
| Validation | 2 | 392 |
| Test | 3 | 802 |
| **Total** | **11** | **3,548** |

### Dataset Visualisation

Exploratory analysis (`experiments/distribution.ipynb`, Matplotlib) revealed a **strong class imbalance**: clause, other and definitions dominate, while recitals and sub-headings are rare. This directly informed two decisions — weighted class loss was tested when fine-tuning Legal-BERT, and macro-F1 (which treats all classes equally) was chosen as the primary metric.

### Model Configurations — Three Experimental Conditions

| Condition | Setup |
|---|---|
| **A — Zero-shot GPT 5.4 mini** | Structured output parser (Pydantic) enforcing valid labels, full class taxonomy in the system prompt, temperature 0 for reproducibility. |
| **B — GPT 5.4 mini + RAG** | Same as A, plus ground-truth labelled examples embedded with the local sentence transformer `BAAI/bge-base-en-v1.5` (768 dims) and stored in Qdrant. At inference, the top-k 5 examples with cosine similarity ≥ 0.7 are injected into the prompt. |
| **C — Fine-tuned Legal-BERT** | `nlpaueb/legal-bert-base-uncased` with a classification head fine-tuned to output the seven classes. |

All three conditions were evaluated on the **same held-out test set of 802 segments**, scored against the lawyer-annotated ground truth.

### Fine-Tuning Legal-BERT

**Why Legal-BERT?** It is a bidirectional encoder pre-trained on extensive legal corpora, and prior work showed domain-adapted BERT variants outperforming generalist LLMs on contract clause tasks — making it the strongest non-LLM candidate to test.

**Setup:** Hugging Face `Trainer` API on a Google Colab **L4 GPU**, across **nine distinct fine-tuning experiments** (`experiments/Condition_C_Finetune_GPUONLY.ipynb`), with a fixed seed (67) for reproducibility, max sequence length 512, bf16 precision, 10 epochs and early stopping (patience 2) using the validation set in the evaluation loop.

**Hyperparameters tuned across the nine runs:**

- Learning rate
- Batch size (8 → 4)
- Gradient accumulation steps (4 → 1)
- Weight decay
- Warm-up steps
- Balanced class weights (via a custom weighted cross-entropy `Trainer`) vs uniform weights

**Best configuration:** batch size 8, gradient accumulation 1, learning rate 2e-5 (the rate recommended in the original BERT paper), no weight decay, no warm-up. The final model (`experiments/Condition_C_run_GPU_ONLY.ipynb`) scored **macro-F1 0.51** and **accuracy 0.67** on the test set, with clause precision of 0.91 but lower clause recall (0.77).

### Results & The Winning Model

| Condition | Macro-F1 (test) |
|---|---|
| A — Zero-shot GPT 5.4 mini ✅ **winner** | **0.52** |
| B — GPT 5.4 mini + RAG | 0.53 |
| C — Fine-tuned Legal-BERT | 0.51 |

Although Condition B edged ahead on macro-F1, **Condition A (zero-shot GPT 5.4 mini) was selected for deployment**. The deciding factor was fitness for the downstream task: clause explanation depends on correctly identifying clauses, and Condition A achieved **0.90 precision on the clause class** — higher than the RAG variant — together with higher clause recall than Legal-BERT. In other words, when the deployed system says "this is a clause", it is right 9 times out of 10.

---

## Phase 2 — Production Implementation

### Architecture

![Architecture](https://github.com/raf945/contract-reviewer-mk-src/blob/main/Phase-1-End-to-End-Pipeline.png)

How the components interact, end to end:

1. **Authentication** — the user signs in through Supabase Auth; unauthenticated users cannot reach the analysis APIs (protecting paid API quotas and sensitive documents).
2. **Ingestion** — the React frontend posts the PDF to the FastAPI backend, which uploads it to a DigitalOcean Spaces bucket (S3-compatible, via boto3) and returns a **presigned URL**. The frontend renders the PDF from that URL with `react-pdf`.
3. **Document scanning** — the presigned URL is passed to **Azure Document Intelligence (prebuilt-read model)**, which extracts the current page's text as structured segments.
4. **Classification** — each segment is classified concurrently (Python `asyncio.TaskGroup`) by **GPT 5.4 mini**, the Phase 1 winner, with Pydantic structured outputs guaranteeing one of the seven valid labels.
5. **Embedding & vector storage** — on first sight of a document, the *entire* contract is scanned and every segment is embedded with **OpenAI `text-embedding-3-small` (1536 dimensions)**, then upserted into a **per-document Qdrant collection**.
6. **Retrieval** — for every segment labelled *clause*, a cosine similarity search retrieves the **top-k 7** most similar segments from the document, applying a **similarity threshold of 0.8**.
7. **Explanation** — the clause, its classification and the retrieved context are composed with a fixed, lawyer-reviewed system prompt and sent to **GPT 5.4** (full-size model, chosen for its stronger instruction-following on nuanced legal language). It returns a plain-English explanation plus a "Client Risk" analysis.
8. **Presentation** — the frontend highlights each clause on the PDF and draws arrows linking highlights to their explanation cards in a side panel.

### Tech Stack

| Layer | Technology |
|---|---|
| **Languages** | Python, TypeScript |
| **Backend** | FastAPI, Uvicorn, Pydantic (structured LLM outputs & API models), asyncio for concurrent LLM calls |
| **Frontend** | React 19, Vite, Tailwind CSS 4, shadcn/ui + Radix UI, react-pdf, react-xarrows, React Router, Lucide & Hugeicons |
| **Authentication** | Supabase Auth (JWT sessions) |
| **File storage** | DigitalOcean Spaces (S3-compatible), accessed with boto3, presigned URLs |
| **Document ingestion** | Azure Document Intelligence (prebuilt-read) |
| **Vector database** | Qdrant (cloud) — per-document collections, cosine distance |
| **LLMs in production** | GPT 5.4 mini (segment classifier), GPT 5.4 (clause explainer), OpenAI `text-embedding-3-small` (1536-d embeddings) |
| **Models evaluated in Phase 1** | Zero-shot GPT 5.4 mini · GPT 5.4 mini + RAG (`BAAI/bge-base-en-v1.5` embeddings) · fine-tuned Legal-BERT (`nlpaueb/legal-bert-base-uncased`) |
| **ML / data libraries** | PyTorch, Hugging Face Transformers, sentence-transformers, scikit-learn, pandas, NumPy, Matplotlib |
| **Annotation** | Label Studio (self-hosted on a DigitalOcean Droplet) |
| **Experimentation** | Jupyter notebooks, Google Colab (L4 GPU) |
| **Deployment** | DigitalOcean App Platform |
| **Tools** | VS Code (with Copilot + shadcn MCP server), Git, GitHub |

### How RAG Is Used

RAG appears in **two distinct roles** across the project:

- **Phase 1 (classification support):** ground-truth *labelled* examples from the training set were embedded and retrieved as few-shot context to test whether retrieval improves classification accuracy (Condition B).
- **Phase 2 (explanation grounding):** the *unlabelled* full document is embedded into a per-PDF Qdrant collection. When explaining a clause, the system retrieves the most semantically similar passages from elsewhere in the same contract (top-k 7, cosine ≥ 0.8) — definitions, related obligations, cross-referenced terms — so GPT 5.4's explanation is grounded in the document itself rather than relying on its general knowledge. This mirrors how a lawyer reads a clause: in the context of the whole agreement, reducing hallucination risk.

### Deployment

The application is deployed on **DigitalOcean App Platform** as a proof of concept:

- The platform connects directly to the GitHub repository — pushes trigger an automatic build and deployment of the Vite-built frontend and the FastAPI backend service.
- Secrets (OpenAI, Azure, Qdrant, Spaces and Supabase credentials) are injected as environment variables rather than committed to the repository.
- A `/health` endpoint exposes a healthcheck used to verify the backend is live after each deploy.
- Keeping storage (Spaces), annotation (Droplet) and hosting (App Platform) in one cloud ecosystem kept the proof of concept simple to operate and demo to legal practitioners.

---

## Project Structure

```
├── backend/              # FastAPI app
│   └── app/
│       ├── agent/        # GPT classification, RAG retrieval & explanation logic
│       ├── models/       # Pydantic schemas (segments, explanations, healthcheck)
│       └── routers/      # /api/files (Spaces upload) & /api/analyse (Azure + LLM)
├── frontend/
│   └── contract-reviewer/  # React + TypeScript + Vite app
├── data/                 # Annotated contract CSVs (train / validation / test) & results
├── experiments/          # Phase 1 notebooks: cleaning, distributions, Conditions A/B/C, evaluation
└── legacy/               # Earlier experiment runs
```

## Evaluation Highlights

- All three classifiers were compared on an identical, document-held-out test set against lawyer-annotated ground truth — macro-F1 as the primary metric, plus per-class precision/recall/F1 and confusion matrices.
- The deployed explanation pipeline was qualitatively reviewed by the two legal annotators across nine clause-explanation sets; both found the explanations accurate and easy to comprehend.
- Notable finding: the generalist zero-shot LLM matched or beat the domain-adapted, fine-tuned Legal-BERT on this task — likely due to the modest fine-tuning dataset size (2,354 segments) and class imbalance — a result that runs counter to parts of the prior literature.

## Acknowledgements

Contracts were annotated by a qualified UK lawyer and a second-year law student. System prompts were drafted with LLM assistance and reviewed by a legal practitioner. Built as a BSc (Hons) Computer Science final-year project (2026).

## Getting Started

### Prerequisites

- Python 3.10+
- Node.js 18+
- (Optional) CUDA-compatible GPU for model fine-tuning, Other wise Open both Condition_C notebooks
 in google colab and use T4 GPU.


### 1. Backend
Open one terminal window and input the following commands

```bash
cd backend
pip install -r requirements.txt
uvicorn app.main:app --reload
```

### 2. Frontend
Open another terminal window and input the following commands
```bash
cd frontend/contract-reviewer
npm install
npm run dev
```

Both terminal windows must be open and running the appropriate commands

When you want to end the session, press CRTL + C at the same time in both terminals

### Environment Variables

For backend ENV variables, open .env and fill out with your API keys or request them from S5633681 Raf Christensen
For frontend ENV variables, open frontend/contract-reviewer/.env.local fill out with your API keys or request them from S5633681 Raf Christensen


## Experiments

The `experiments/` directory contains Jupyter notebooks documenting the model development lifecycle:

| Notebook | Purpose |
|---|---|
| `clean_data.ipynb` | Data preprocessing and cleaning pipeline |
| `Condition_A.ipynb` | Baseline experiment setup |
| `Condition_B.ipynb` | Alternate training configuration |
| `Condition_C_Finetune_GPUONLY.ipynb` | Legal-BERT fine-tuning |
| `Condition_C_run_GPU_ONLY.ipynb` | Legal-BERT test run |
| `distribution.ipynb` | Dataset distribution analysis |
| `evaluation.ipynb` | Model performance evaluation |

Results and model artifacts are stored under `experiments/legal-bert-results/`.

## License

This project is proprietary. All rights reserved.
